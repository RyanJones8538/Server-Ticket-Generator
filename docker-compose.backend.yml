services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./servers:/app/servers:ro
      - ./config:/app/config:ro
      - ./tickets:/app/tickets:ro
